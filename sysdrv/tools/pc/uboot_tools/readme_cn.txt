mkimage工具安装方法:

方法一：
    sudo apt-get install uboot-mkimage
or
    sudo apt-get install u-boot-tools:i386

方法二：
    1 编译uboot源码
    2 cp source/uboot/u-boot/tools/mkimage /usr/bin/

mkenvimage 工具安装方法:
    1 编译uboot源码
    2 cp source/uboot/u-boot/tools/mkenvimage /usr/bin/
